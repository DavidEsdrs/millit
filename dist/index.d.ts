export = millit;
/**
 * Parse val to milliseconds
 *
 *
 * @param {String|Number} time
 * @throws {Error} throw an error if val is an empty string or is not a number
 * @return {String|Number}
 */
declare function millit(time: string | number): string | number;
